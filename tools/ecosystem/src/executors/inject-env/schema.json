{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Inject Env File",
  "description": "Inject .env file into the provided app",
  "type": "object",
  "properties": {
    "scope": {
      "type": "string",
      "description": "The scope (e.g. 'www') to locate the env library."
    },
    "environment": {
      "type": "string",
      "enum": ["development", "production"],
      "description": "Which env file to decrypt."
    },
    "type": {
      "type": "string",
      "description": "The type of project (e.g app, lib, tool)"
    },
    "projectName": {
      "type": "string",
      "description": "The project name where to deploy the .env file"
    }
  },
  "required": ["scope"]
}
